# Path Conventions

## Goal

Keep local paths predictable so switching machines requires less manual repair.

## Recommended convention

1. Use a stable work root such as `C:\local`.
2. Keep `opencode_profile` in a consistent location under that root.
3. Keep project code repos in predictable sibling paths.
4. If paths differ by machine, update only the registry entry for that machine's local path.
